If emailType == "event-blast" or "listicle":
“You’re summarizing an event-blast. First, write a 1–2 sentence intro. Then output each event as a bullet in this
format:
- [Date]: [Event Name] @ [Location]
Limit to the first 10 items; if more exist, append “…and N more.””
Else:
“Summarize this email in 2–3 sentences highlighting the core insight.”
# Pre-Summarization Step: Summarize Each Email Based on Type

1. Pre-clean bodyText:
- Remove any standalone lines matching /^\[Image\]$/i.
- Strip out attachments, embedded images, and long signatures.

2. Extract links:
- Parse all URLs from the original bodyText using a regex like /(https?:\/\/\S+)/g.
- Store the result as an array `links[]`.

3. Classify emailType:
- Determine if the email is an "event-blast" or "listicle".
- Otherwise treat it as narrative/news-brief.

4. Summarize by branch:
- If emailType == "event-blast" or "listicle":
“You’re summarizing an event-blast. First, write a 1–2 sentence introduction. Then output each event as a bullet in this
format:
- [Date]: [Event Name] @ [Location]
Limit to the first 10 items; if more exist, append “…and N more.””
- Else:
“Summarize this email in 2–3 sentences highlighting the core insight or main takeaway.”

5. Output:
- Construct `summaryText` using this exact format:
From: [sender email or name]
Date: [email date]
Subject: [email subject]
Content: [the generated summary or bullet list]
Links: [list each URL from `links[]` on its own line]
- Ensure each field is on its own line, with no extra indentation.
- Store this structured text as `summaryText` for downstream use.